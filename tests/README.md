This directory vendors and adapts parts of the upstream plotnine test suite.

The goal is not to run 100% of upstream tests immediately, but to use the suite
as a compatibility target and incrementally make tests pass.

